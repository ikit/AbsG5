export class Notification {
    icon = "";      // L'icone à afficher pour cette notificatio
    url = "";       // L'url de redirection vers la zone notifié (peut-être null)
    label = "";     // Le text affiché à l'utilisateur pour la notification
}
